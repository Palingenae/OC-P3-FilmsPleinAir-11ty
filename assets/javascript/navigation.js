const navMenu = document.querySelector('.navigation__menu--mobile');
const iconMenu = document.querySelector('.icon--menu--mobile');

function toggleMenu() {
    if (iconMenu.classList.contains(".icon--menu--mobile--active")) {
        iconMenu.classList.replace("")
    }
}